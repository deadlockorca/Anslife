import LegacyNextEntry from '../../legacy/LegacyNextEntry';

export const dynamicParams = true;
export const revalidate = 0;

export default function LegacyCatchAllPage() {
  return <LegacyNextEntry />;
}
